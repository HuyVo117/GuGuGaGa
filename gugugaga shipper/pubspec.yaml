name: gugugaga_shipper
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  geolocator: ^12.0.0
  google_maps_flutter: ^2.7.0
  flutter_dotenv: ^5.2.1
  shared_preferences: ^2.2.3
  flutter_map: ^7.0.2
  flutter_compass: ^0.8.1
  latlong2: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
